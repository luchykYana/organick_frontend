import {NotFound} from '../../components';

const NotFoundPage = () => {
    return (
        <div className={`grid-content-full`}>
            <NotFound/>
        </div>
    );
};

export {NotFoundPage};